---
# vars file for tmux

# default tmux configuration path
__default_tmux_conf_dir: "{{ ansible_facts['env']['HOME'] }}/.tmux"

# default tmux plugin path
__default_tmux_plugin_dir: "{{ __default_tmux_conf_dir }}/plugins"

