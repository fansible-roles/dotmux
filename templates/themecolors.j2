# === Tmux Theme and Colors ===
#
# define the default status-bar background color
set -g status-bg '#4e4e4e' # dracula gray

# define the default status-bar foreground color (the color for letters)
set -g status-fg '#00ff00' # light green

# Set active current pane number color
set -g display-panes-active-colour '#bd93f9'

# Set incative display pane number color
set -g display-panes-colour '#56406a' # another good choice is #6e5883

#######################################
# --- Dracula Theme Settings ---
#######################################
# enable powerline symbols
set -g @dracula-show-powerline 'true' 

# Switch the left smiley icon
# it can accept `session`, `smiley`, `window`, or any character.
# set -g @dracula-show-left-icon session
set -g @dracula-show-left-icon ' '

# show the window status flags
set -g @dracula-show-flags 'true'

# enables dracula plugins
set -g @dracula-plugins 'cpu-usage battery time'

# show load average instead of CPU percentage
set -g @dracula-cpu-display-load 'true'

# define the cpu usage background color to `pink` and foreground to `gray`
set -g @dracula-cpu-usage-colors 'gray light_purple'

# define the CPU label
set -g @dracula-cpu-usage-label ''

# define the battery plugin background and foreground colors
set -g @dracula-battery-colors 'gray green'

# define the battery plugin label
set -g @dracula-battery-label ''

# enable high constrast pane border
set -g @dracula-border-contrast 'true'

# define the current window backgroun color to `dark_purple` and foreground to `cyan`
# this option usually is define with: 
# `set-window-option -g window-status-current-style fg='#0dc3c5',bg='#4e4e4e'
# however dracula takes precendence over this option by default
set -g @dracula-current-window-colors 'dark_purple cyan'

# define the inactive window colors, same as: 
# `set-window-option -g window-status-style bg=color,fg=color`
# however dracula is using the window-status-format which takes precedence
set -g @dracula-window-colors 'gray pink'

# disable timezone in the time plugin
set -g @dracula-show-timezone 'false'

# set time clock to be 24 hours clock
set -g @dracula-military-time 'true'

# define day/month instead of month/day
set -g @dracula-day-month 'true'
